# Normalise line endings so the starter reads the same on macOS, Linux and
# Windows. Without this, a candidate cloning on Windows gets CRLF in every
# file and their first commit shows every line as changed.
* text=auto eol=lf

*.ico binary
*.png binary
*.jpg binary
*.woff2 binary
*.db binary
